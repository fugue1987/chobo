# chobo CRM 配置（裸 Node 部署）。
# 用法：cp chobo-crm.env.example chobo-crm.env，改下面两个【必填】，然后 ./start.sh
# 取值直接粘贴原文，不要加引号；含特殊字符的密码也原样粘。

# 【必填】指向你自己的 Postgres。空库即可——CRM 启动会自动建表 + 灌价目表，绝不碰你的其它库。
CHOBO_DATABASE_URL=postgresql://chobo:你的密码@127.0.0.1:5432/chobo

# 【必填】ingest 闸门（强随机串）。业务服务（用了 chobo SDK 的那个）的 CHOBO_INGEST_SECRET 必须与此【完全相同】。
CHOBO_INGEST_SECRET=改成一串强随机字符

# ── 以下都可选，留默认即可 ──

# 监听地址/端口（默认 0.0.0.0:8787）。仅本机/内网访问建议绑 127.0.0.1（见 README「安全」）。
# CHOBO_HOST=0.0.0.0
# CHOBO_PORT=8787

# 请求载荷留存策略：off | metadata（默认） | truncated
# CHOBO_PAYLOAD_MODE=metadata

# 高级：价目表 / 看板目录路径。start.sh 已按解压位置自动设好，一般无需填。
# CHOBO_PRICE_SEED=/abs/path/to/price-seed.json
# CHOBO_WEB_DIR=/abs/path/to/web

# （可选）价目热载轮询间隔(秒)。默认 60:改价后运行中的 CRM 最迟 60s 自动生效。
# 设 0 关闭轮询(退回"仅开机加载一次",改价需重启进程)。
# CHOBO_PRICE_REFRESH_SEC=60
